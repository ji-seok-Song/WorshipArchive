import SwiftUI

enum ArchiveTheme {
    static let tint = Color(red: 0.24, green: 0.25, blue: 0.62)
    static let accent = Color(red: 0.83, green: 0.48, blue: 0.18)
    static let background = Color(uiColor: .systemGroupedBackground)
    static let surface = Color(uiColor: .secondarySystemGroupedBackground)
}
